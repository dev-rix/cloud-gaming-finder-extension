// This is the only catalog endpoint the extension needs to know about.
// The manifest points to provider files, so adding a provider normally does
// not require changing this file or the extension host permissions.
export const CATALOG_MANIFEST_URL = "https://raw.githubusercontent.com/dev-rix/cloud-gaming-finder-catalog/master/catalog/manifest.json";
